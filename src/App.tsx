export default function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 px-6 text-center">
      <span className="rounded-full border border-subtle px-3 py-1 text-xs font-medium tracking-widest text-muted uppercase">
        Local-first · No accounts · No tracking
      </span>
      <h1 className="font-mono text-6xl font-bold sm:text-7xl">
        key<span className="text-primary">flow</span>
      </h1>
      <p className="max-w-md text-muted">
        A clean, game-like typing trainer that builds real speed and accuracy —
        and shows you exactly which keys are holding you back.
      </p>
      <p className="font-mono text-sm text-subtle">
        the type test is coming together…
      </p>
    </main>
  )
}
